<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.flowmate.attendance.mapper.LeaveUsageMapper">

    <!-- 계획서 4 D2 — 제약 위반을 잡지 않는다. 먼저 조회해서 분기한다 -->
    <select id="existsByApprovalId" resultType="boolean">
        SELECT EXISTS (SELECT 1 FROM leave_usage WHERE approval_id = #{approvalId})
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="usageId">
        INSERT INTO leave_usage (emp_id, approval_id, leave_type, start_date, end_date, days, applied_at)
        VALUES (#{empId}, #{approvalId}, #{leaveType}, #{startDate}, #{endDate}, #{days}, #{appliedAt})
    </insert>

    <select id="findByApprovalId" resultType="LeaveUsage">
        SELECT usage_id, emp_id, approval_id, leave_type, start_date, end_date, days, applied_at
          FROM leave_usage
         WHERE approval_id = #{approvalId}
    </select>

</mapper>
