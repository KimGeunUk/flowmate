<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.flowmate.approval.mapper.RejectHistoryMapper">

    <!--
      Phase 5 AI 사전점검의 학습 원천에 한 줄을 쌓는다.
    -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO approval_reject_history
            (approval_id, doc_type, dept_id, rejector_id, reason_category, reason_text, rejected_at)
        VALUES
            (#{approvalId}, #{docType}, #{deptId}, #{rejectorId},
             #{reasonCategory}, #{reasonText}, #{rejectedAt})
    </insert>

    <!--
      ★ reason_text 를 SELECT 목록에 넣지 않는다 - 계획서 5 Task 5 (개인정보 유입 방지).
      idx_reject_type_dept(doc_type, dept_id, rejected_at DESC) 인덱스를 그대로 탄다.
    -->
    <select id="findRecentReasonCategories" resultType="string">
        SELECT reason_category
          FROM approval_reject_history
         WHERE doc_type = #{docType}
           AND dept_id = #{deptId}
         ORDER BY rejected_at DESC
         LIMIT #{limit}
    </select>

    <select id="findRecentReasonCategoriesCompanyWide" resultType="string">
        SELECT reason_category
          FROM approval_reject_history
         WHERE doc_type = #{docType}
         ORDER BY rejected_at DESC
         LIMIT #{limit}
    </select>

</mapper>
