<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.flowmate.org.mapper.DepartmentMapper">

    <!--
      조직도 계층 조회 (재귀 CTE).

      형제 정렬을 sort_order 로 하는 이유:
      department.sort_order 를 NOT NULL 로 선언한 목적이 형제 정렬인데,
      path 를 dept_id 로만 만들면 정렬이 id 순이 되어 그 컬럼이 무의미해진다.
      sort_order 를 앞에 두고 dept_id 를 tie-breaker 로 붙인다.

      LPAD 로 자릿수를 맞추는 이유: 문자열 비교이므로 '10' < '9' 가 되는 것을 막는다.

      use_yn = 'Y' 를 anchor 와 recursive term 양쪽에 두면
      사용하지 않는 부서의 하위 부서까지 자연히 제외된다 (서브트리에 도달하지 못한다).

      Oracle 대응은 docs/oracle-mapping.md 참조 (WITH RECURSIVE → CONNECT BY PRIOR).
    -->
    <select id="findDeptTree" resultType="DeptTreeItem">
        WITH RECURSIVE dept_tree AS (
            SELECT d.dept_id,
                   d.parent_dept_id,
                   d.dept_name,
                   d.dept_code,
                   1 AS depth,
                   LPAD(CAST(d.sort_order AS VARCHAR(5)), 5, '0') || '-' ||
                   LPAD(CAST(d.dept_id    AS VARCHAR(10)), 10, '0') AS sort_path
              FROM department d
             WHERE d.parent_dept_id IS NULL
               AND d.use_yn = 'Y'
            UNION ALL
            SELECT c.dept_id,
                   c.parent_dept_id,
                   c.dept_name,
                   c.dept_code,
                   t.depth + 1,
                   t.sort_path || '>' ||
                   LPAD(CAST(c.sort_order AS VARCHAR(5)), 5, '0') || '-' ||
                   LPAD(CAST(c.dept_id    AS VARCHAR(10)), 10, '0')
              FROM department c
              JOIN dept_tree t ON c.parent_dept_id = t.dept_id
             WHERE c.use_yn = 'Y'
        )
        SELECT t.dept_id,
               t.parent_dept_id,
               t.dept_name,
               t.dept_code,
               t.depth,
               (SELECT COUNT(*)
                  FROM employee e
                 WHERE e.dept_id = t.dept_id
                   AND e.use_yn = 'Y') AS emp_count
          FROM dept_tree t
         ORDER BY t.sort_path
    </select>

    <!--
      부서장 체인 조회 — 조직도의 재귀 CTE 를 방향만 뒤집었다.
      조직도는 루트에서 아래로 내려가고, 이것은 주어진 부서에서 위로 올라간다.
      JOIN 조건이 p.dept_id = c.parent_dept_id 인 것이 그 차이다.

      각 부서에서 최고 직급 1명만 뽑기 위해 ROW_NUMBER() 로 부서별 순위를 매긴다.
      동급이 있으면 입사일 이른 사람, 그래도 같으면 emp_id 로 결정한다 —
      결재선이 실행마다 달라지지 않게 하는 안정 정렬이다.

      ORDER BY depth 로 가까운 부서가 먼저 오게 한다. 결재선 정책이 이 순서를 전제한다.
    -->
    <select id="findDeptHeadChain" resultType="Employee">
        WITH RECURSIVE up_chain AS (
            SELECT d.dept_id, d.parent_dept_id, 1 AS depth
              FROM department d
             WHERE d.dept_id = #{deptId}
               AND d.use_yn = 'Y'
            UNION ALL
            SELECT p.dept_id, p.parent_dept_id, c.depth + 1
              FROM department p
              JOIN up_chain c ON p.dept_id = c.parent_dept_id
             WHERE p.use_yn = 'Y'
        ),
        ranked AS (
            SELECT e.emp_id, e.emp_no, e.emp_name, e.dept_id, e.position_id,
                   e.email, e.hire_date, e.role, e.use_yn,
                   d.dept_name, p.position_name, p.position_level,
                   u.depth,
                   ROW_NUMBER() OVER (PARTITION BY u.dept_id
                                      ORDER BY p.position_level DESC, e.hire_date, e.emp_id) AS rn
              FROM up_chain u
              JOIN employee   e ON e.dept_id     = u.dept_id AND e.use_yn = 'Y'
              JOIN department d ON d.dept_id     = e.dept_id
              JOIN position   p ON p.position_id = e.position_id
        )
        SELECT r.emp_id, r.emp_no, r.emp_name, r.dept_id, r.position_id,
               r.email, r.hire_date, r.role, r.use_yn,
               r.dept_name, r.position_name, r.position_level
          FROM ranked r
         WHERE r.rn = 1
         ORDER BY r.depth
    </select>

</mapper>
